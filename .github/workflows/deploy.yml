name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - 'src/**'
      - 'examples/**'
      - 'static/**'
      - 'docusaurus.config.js'
      - 'package*.json'
      - 'sidebars.js'
      - '.github/workflows/**'

jobs:
  build:
    name: Build Docusaurus
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      
      - name: Detect changes in examples folder
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            examples:
              - 'examples/**'
      
      - name: Set up Quarto
        if: steps.filter.outputs.examples == 'true'
        uses: quarto-dev/quarto-actions/setup@v2
      
      - name: Install Quarto colab extension
        if: steps.filter.outputs.examples == 'true'
        run: quarto add coatless-quarto/colab --no-prompt
        working-directory: examples
      
      - name: Render Quarto examples
        if: steps.filter.outputs.examples == 'true'
        run: quarto render
        working-directory: examples
      
      - name: Install dependencies
        run: npm ci
      
      - name: Clean API docs
        run: npx docusaurus clean-api-docs algopack
      
      - name: Generate API docs
        run: npx docusaurus gen-api-docs algopack
      
      - name: Build website
        run: npm run build
      
      - name: Upload Build Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4